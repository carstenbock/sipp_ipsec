<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  VoLTE IMS Registration with IPSec (3GPP TS 33.203)

  This scenario simulates a VoLTE UE performing IMS registration
  with IPSec security mechanism agreement (sec-agree).

  The flow:
    1. Send initial REGISTER with Security-Client header (unprotected)
    2. Receive 401 Unauthorized with AKA challenge + Security-Server
    3. Set up IPSec SAs using CK/IK from AKA (via ipsec_setup action)
    4. Send second REGISTER over IPSec with Authorization + Security-Verify

  Usage:
    sudo ./sipp -sf volte_register.xml \
      -ipsec \
      -au <imsi> -ap <password> \
      -key aka_K <hex_K> -key aka_OP <hex_OP> -key aka_AMF <hex_AMF> \
      <pcscf_ip>:<pcscf_port>

  The -ipsec flag enables IPSec SA management.
  AKA keys (K, OP, AMF) can be provided via -key or inline in the
  [authentication] keyword.
  Requires root or CAP_NET_ADMIN for XFRM SA/SP creation.
-->

<scenario name="VoLTE IMS Registration with IPSec">

  <!-- Step 1: Initial REGISTER (unprotected) with Security-Client -->
  <send retrans="500">
    <![CDATA[

      REGISTER sip:[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Route: <sip:[remote_ip]:[remote_port];lr>
      From: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>;tag=[call_number]
      To: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=600000;+g.3gpp.icsi-ref="urn%3Aurn-7%3A3gpp-service.ims.icsi.mmtel";+sip.instance="<urn:gsma:imei:35609204-079054-0>"
      Supported: path, gruu, sec-agree
      Require: sec-agree
      Proxy-Require: sec-agree
      Security-Client: [security_client]
      Authorization: Digest username="[service]@ims.mnc001.mcc001.3gppnetwork.org",realm="ims.mnc001.mcc001.3gppnetwork.org",uri="sip:ims.mnc001.mcc001.3gppnetwork.org",nonce="",response=""
      Max-Forwards: 70
      Allow: INVITE, ACK, BYE, CANCEL, UPDATE, PRACK, NOTIFY, MESSAGE, REFER, OPTIONS
      User-Agent: SIPp-VoLTE/1.0
      Expires: 600000
      Content-Length: 0

    ]]>
  </send>

  <!-- Step 2: Receive 401 with AKA challenge + Security-Server -->
  <!-- The auth="true" flag saves the WWW-Authenticate header.
       The ipsec_setup action parses Security-Server and creates XFRM SAs. -->
  <recv response="401" auth="true">
    <action>
      <ipsec_setup />
    </action>
  </recv>

  <!-- Step 3: Re-REGISTER over IPSec-protected path -->
  <!-- After ipsec_setup, the socket is rebound to the protected client port.
       Traffic is now ESP-encapsulated by the kernel XFRM subsystem. -->
  <send retrans="500">
    <![CDATA[

      REGISTER sip:[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Route: <sip:[remote_ip]:[remote_port];lr>
      From: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>;tag=[call_number]
      To: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=600000;+g.3gpp.icsi-ref="urn%3Aurn-7%3A3gpp-service.ims.icsi.mmtel";+sip.instance="<urn:gsma:imei:35609204-079054-0>"
      [authentication username=[service]@ims.mnc001.mcc001.3gppnetwork.org]
      Supported: path, gruu, sec-agree
      Require: sec-agree
      Proxy-Require: sec-agree
      Security-Client: [security_client]
      Security-Verify: [security_verify]
      Max-Forwards: 70
      Allow: INVITE, ACK, BYE, CANCEL, UPDATE, PRACK, NOTIFY, MESSAGE, REFER, OPTIONS
      User-Agent: SIPp-VoLTE/1.0
      Expires: 600000
      Content-Length: 0

    ]]>
  </send>

  <!-- Step 4: Receive 200 OK (IPSec-protected) -->
  <recv response="200">
  </recv>

  <!-- Keep registration alive for a while -->
  <pause milliseconds="30000"/>

  <!-- De-REGISTER (IPSec-protected) -->
  <send retrans="500">
    <![CDATA[

      REGISTER sip:[remote_ip] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Route: <sip:[remote_ip]:[remote_port];lr>
      From: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>;tag=[call_number]
      To: <sip:[service]@ims.mnc001.mcc001.3gppnetwork.org>
      Call-ID: [call_id]
      CSeq: 3 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>;expires=0
      [authentication username=[service]@ims.mnc001.mcc001.3gppnetwork.org]
      Supported: path, gruu, sec-agree
      Require: sec-agree
      Proxy-Require: sec-agree
      Security-Client: [security_client]
      Security-Verify: [security_verify]
      Max-Forwards: 70
      Allow: INVITE, ACK, BYE, CANCEL, UPDATE, PRACK, NOTIFY, MESSAGE, REFER, OPTIONS
      User-Agent: SIPp-VoLTE/1.0
      Expires: 0
      Content-Length: 0

    ]]>
  </send>

  <!-- Receive 200 OK for de-registration -->
  <recv response="200">
    <action>
      <ipsec_teardown />
    </action>
  </recv>

  <!-- Definition of the response time repartition table (unit is ms) -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

  <!-- Definition of the call length repartition table (unit is ms) -->
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
